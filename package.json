{
  "name": "riftbound-collection-tracker",
  "private": true,
  "scripts": {
    "lint": "eslint public/*.js --no-warn-ignored",
    "test": "npm run lint && node tests/site/riftbound_checks.mjs",
    "test:site": "npm test"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.62.1",
    "c8": "12.0.0",
    "eslint": "10.8.1"
  }
}
